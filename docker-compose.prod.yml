services:
  toto-task-app:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - '3001:8000'
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    command: npm run start:prod
